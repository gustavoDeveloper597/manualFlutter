lib/
  src/
    models/-- clases que se ocupan para crear objetos que usaremos en el app
      models.dart //--- sirve para exportar todos los models
    routes/
      AppRoutes.dart //clase que regresa la ruta inicial,rutas etc
    screens/
      screens.dart //manejas las exportaciones
    theme/
      AppTheme.dart // maneja todos los estilos de la app en general 
    widgets/
      widgets.dart // exporta todos los widgets que creamos para nuestra app
  main.dart //---- archivo principal   en donde se encuentra runApp(---->    
    
  
